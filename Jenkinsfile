node {
    checkout scm

    stages {
        stage('Build') {
            steps {
                echo 'starting build pipeline'
                //sh 'make'
                //archiveArtifacts artifacts: '**/target/*.jar', fingerprint: true
            }
        }
    }
}
